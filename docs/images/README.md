# Diagram Assets (Optional)

This folder is reserved for optional PNG/SVG copies of diagrams used in the root README.

- `business-value.png` (optional)
- `logical-architecture.png` (optional)
- `architecture.png` (optional)

Mermaid diagrams are embedded directly in the Markdown files so the repository remains usable even without image files.
